# ...existing code...
name: CI/CD - FastAPI Deployment

on:
  push:
    branches:
      - feature/devops-platform
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: deploy-${{ github.ref }}
      cancel-in-progress: true

    env:
      VM_IP: ${{ secrets.VM_IP }}
      SSH_USER: ${{ secrets.SSH_USER }}
      REPO_PATH: /home/azureuser/enterprise-devops-ml-platform
      BRANCH: feature/devops-platform
      MAX_HEALTH_RETRIES: 15
      HEALTH_SLEEP_SEC: 2

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Validate required secrets
        run: |
          if [ -z "${{ secrets.VM_IP }}" ] || [ -z "${{ secrets.SSH_USER }}" ] || [ -z "${{ secrets.SSH_KEY }}" ]; then
            echo "Missing required secrets: VM_IP, SSH_USER, or SSH_KEY" >&2
            exit 1
          fi

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Deploy to VM
        run: |
          echo "Connecting to VM and running deployment..."
          ssh -o StrictHostKeyChecking=no "$SSH_USER@$VM_IP" <<'REMOTE'
            set -euo pipefail

            cd /home/azureuser/enterprise-devops-ml-platform

            echo "Resetting to remote branch..."
            git fetch origin --depth=1
            git reset --hard origin/feature/devops-platform

            # Safe kill any process on port 8000 (won't fail if lsof missing)
            if command -v lsof >/dev/null 2>&1; then
              PID=$(lsof -ti:8000 || true)
              if [ -n "$PID" ]; then
                echo "Killing process(es) on port 8000: $PID"
                kill -9 $PID || true
              fi
            else
              echo "lsof not found on remote; skipping port kill."
            fi

            echo "Stopping existing Docker containers..."
            docker-compose down || true

            echo "Building and starting containers..."
            docker-compose up -d --build

            echo "Executing optional deployment.sh (if present)..."
            if [ -f deployment.sh ]; then
              chmod +x deployment.sh
              ./deployment.sh
            else
              echo "No deployment.sh found, skipping."
            fi

            echo "Remote deploy finished."
          REMOTE

      - name: Confirm deployment (health check)
        run: |
          echo "Waiting for API to become healthy..."
          for i in $(seq 1 $MAX_HEALTH_RETRIES); do
            if curl -fsS http://$VM_IP:8000/health >/dev/null 2>&1; then
              echo "✅ API is healthy (attempt $i)"
              exit 0
            fi
            echo "⏳ attempt $i/$MAX_HEALTH_RETRIES - waiting $HEALTH_SLEEP_SEC seconds..."
            sleep $HEALTH_SLEEP_SEC
          done
          echo "❌ Health check failed after $MAX_HEALTH_RETRIES retries" >&2
          exit 1

      - name: Deployment complete
        run: echo "🎉 Deployment to $VM_IP completed successfully."
# ...existing code...
